# Arvix v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Собрать офлайн-PWA, который в зале показывает упражнение, его технику и норму подходов, принимает отметки о выполнении и сам считает двойную прогрессию.

**Architecture:** Vite + React, сборка в чистую статику без бэкенда. Вся логика — чистые функции в `src/lib/`, они же покрыты тестами; React-компоненты остаются тонкими. Контент разделён на два JSON: библиотека упражнений (как делать) и программа (сколько делать). Прогресс живёт в `localStorage`, офлайн обеспечивает `vite-plugin-pwa` с предварительным кешированием всех гифок.

**Tech Stack:** Vite, React 19, Vitest + jsdom, vite-plugin-pwa, CSS Modules. Без роутера, без стейт-менеджера, без UI-библиотек.

**Спека:** `docs/superpowers/specs/2026-07-29-arvix-design.md`

## Global Constraints

- **Язык интерфейса — русский.** Все пользовательские строки на русском, включая `aria-label`.
- **Никаких сторонних зависимостей сверх перечисленных в Tech Stack.** Роутинг, состояние и таймер пишутся руками — так решено в спеке (раздел 4).
- **Вся логика — в `src/lib/` чистыми функциями.** Компоненты и хуки не содержат вычислений, которые стоило бы протестировать. Это прямое требование спеки, раздел 10.
- **Тёмная тема, крупные элементы.** Минимальная область нажатия — 56 px (переменная `--tap`). Требование спеки, раздел 2: телефон в потной руке.
- **Идентификатор упражнения = имя файла гифки.** `public/gifs/<id>.gif`. Спека, раздел 5.1.
- **Отсутствующая гифка не ломает экран** — показывается заглушка. Спека, раздел 9.
- **Битый `localStorage` не роняет приложение** — состояние сбрасывается в пустое. Спека, раздел 5.3.
- **Каждая задача заканчивается коммитом.** Сообщения на русском, префиксы `feat:` / `test:` / `chore:` / `docs:`.

## Уточнения к спеке

Два места, где план конкретизирует спеку. Оба — следствие требований самой спеки, не изменение решений.

1. **`current.athletic` добавлен в схему `localStorage`.** Спека (раздел 7) требует отметку на атлетическое упражнение целиком, но схема в разделе 5.3 её не показывает. Добавляем поле `athletic: { [exerciseId]: true }` внутрь `current`.

2. **Состояние тренировки живёт в `src/lib/workout.js`, а `src/hooks/useWorkout.js` — тонкая обёртка над ним.** Спека в разделе 4 называет файл хука, а в разделе 10 требует тестов на его логику. Тестировать React-хук — значит тянуть `@testing-library/react`; вместо этого логика уезжает в чистые функции, а хук остаётся проводкой. Это следует правилу самой спеки: «вся логика вынесена в `src/lib/` чистыми функциями».

## Карта файлов

| Файл | Ответственность |
|---|---|
| `package.json`, `vite.config.js`, `index.html` | Сборка, тесты, точка входа |
| `src/lib/storage.js` | Чтение и запись `localStorage`, защита от битого JSON |
| `src/lib/progression.js` | Правило двойной прогрессии, одна чистая функция |
| `src/lib/timer.js` | Редьюсер таймера отдыха и форматирование времени |
| `src/lib/router.js` | Разбор и сборка хеша, подписка на `hashchange` |
| `src/lib/workout.js` | Переходы состояния тренировки: старт, подход, вес, финиш |
| `src/lib/program.js` | Поиск дня по id, вычисление сегодняшнего дня |
| `src/data/exercises.json` | Библиотека: 22 упражнения с техникой |
| `src/data/program.json` | Программа: три дня |
| `src/hooks/useWorkout.js` | Проводка `lib/workout.js` в React |
| `src/hooks/useRestTimer.js` | Проводка `lib/timer.js` + `setInterval` + вибрация |
| `src/components/ExerciseGif.jsx` | Гифка с заглушкой при отсутствии файла |
| `src/components/DayList.jsx` | Главный экран, три дня |
| `src/components/SetTracker.jsx` | Ряд кружков-подходов и правка повторов |
| `src/components/ExerciseRow.jsx` | Строка упражнения на экране тренировки |
| `src/components/RestTimer.jsx` | Панель таймера снизу |
| `src/components/ExerciseDetail.jsx` | Полноэкранная карточка техники |
| `src/components/AthleticBlock.jsx` | Финишер |
| `src/components/WorkoutScreen.jsx` | Экран тренировки целиком + итоговая плашка |
| `src/App.jsx` | Роутинг между тремя экранами |

Стили — CSS Modules рядом с компонентом (`Component.module.css`), общие токены в `src/index.css`.

---

### Task 1: Каркас проекта и тулинг

**Files:**
- Create: `package.json`, `vite.config.js`, `index.html`, `.gitignore`
- Create: `src/main.jsx`, `src/App.jsx`, `src/index.css`
- Test: `src/lib/smoke.test.js`

**Interfaces:**
- Consumes: ничего
- Produces: рабочие команды `npm run dev`, `npm run build`, `npm test`

- [ ] **Step 1: Создать `package.json`**

```json
{
  "name": "arvix",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

- [ ] **Step 2: Установить зависимости**

Версии не пиньтся вручную — npm поставит актуальные и запишет их сам.

```bash
cd /var/www/arvix
npm install react react-dom
npm install -D vite @vitejs/plugin-react vitest jsdom vite-plugin-pwa
```

- [ ] **Step 3: Проверить, что установилось**

Run: `npm ls react vite vitest --depth=0`
Expected: три пакета с конкретными версиями, без `UNMET DEPENDENCY`.

- [ ] **Step 4: Создать `vite.config.js`**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

- [ ] **Step 5: Создать `index.html`**

`viewport-fit=cover` нужен, чтобы панель таймера умела уезжать под вырез и системную полосу.

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="theme-color" content="#0f1115" />
    <title>Arvix</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 6: Создать `src/index.css` с токенами**

```css
:root {
  --bg: #0f1115;
  --surface: #171a21;
  --surface-2: #212632;
  --line: #2a3040;
  --text: #e9ecf1;
  --text-dim: #939bab;
  --accent: #c6f24e;
  --accent-ink: #16200a;
  --danger: #ef6b6b;
  --tap: 56px;
  --radius: 14px;
  --font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, system-ui, sans-serif;
}

* { box-sizing: border-box; }

html, body, #root {
  margin: 0;
  min-height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font);
  -webkit-font-smoothing: antialiased;
}

body {
  overscroll-behavior-y: none;
}

button {
  font: inherit;
  color: inherit;
  border: none;
  background: none;
  cursor: pointer;
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
  user-select: none;
}
```

- [ ] **Step 7: Создать `src/main.jsx` и заглушку `src/App.jsx`**

```jsx
// src/main.jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.jsx'
import './index.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

```jsx
// src/App.jsx
export default function App() {
  return <h1>Arvix</h1>
}
```

- [ ] **Step 8: Написать дымовой тест**

```js
// src/lib/smoke.test.js
import { describe, it, expect } from 'vitest'

describe('тулинг', () => {
  it('запускает тесты и видит браузерное окружение', () => {
    expect(typeof localStorage.setItem).toBe('function')
  })
})
```

- [ ] **Step 9: Прогнать тест**

Run: `npm test`
Expected: PASS, 1 тест. Если падает с «localStorage is not defined» — не подхватился `environment: 'jsdom'` из `vite.config.js`.

- [ ] **Step 10: Проверить сборку**

Run: `npm run build`
Expected: успешная сборка, появляется каталог `dist/`.

- [ ] **Step 11: Дополнить `.gitignore` и закоммитить**

`.gitignore` уже содержит `node_modules`, `dist`, `.DS_Store` — проверить и дополнить при необходимости.

```bash
git add -A
git commit -m "chore: каркас проекта — Vite, React, Vitest"
```

---

### Task 2: `lib/storage.js` — чтение и запись localStorage

**Files:**
- Create: `src/lib/storage.js`
- Test: `src/lib/storage.test.js`
- Delete: `src/lib/smoke.test.js` (свою работу отработал)

**Interfaces:**
- Consumes: ничего
- Produces:
  - `loadState(): {version: 1, lastSession: object, current: object|null}`
  - `saveState(state): void`
  - `clearState(): void`
  - `EMPTY_STATE` — константа пустого состояния

- [ ] **Step 1: Написать падающие тесты**

```js
// src/lib/storage.test.js
import { describe, it, expect, beforeEach } from 'vitest'
import { loadState, saveState, clearState } from './storage.js'

const KEY = 'arvix.v1'

describe('storage', () => {
  beforeEach(() => localStorage.clear())

  it('на пустом хранилище отдаёт пустое состояние', () => {
    expect(loadState()).toEqual({ version: 1, lastSession: {}, current: null })
  })

  it('сохранённое состояние читается обратно', () => {
    const state = {
      version: 1,
      lastSession: { 'back-squat': { weight: 80, reps: [8, 8, 8], date: '2026-07-21' } },
      current: null,
    }
    saveState(state)
    expect(loadState()).toEqual(state)
  })

  it('битый JSON не роняет приложение', () => {
    localStorage.setItem(KEY, '{это не json')
    expect(() => loadState()).not.toThrow()
    expect(loadState()).toEqual({ version: 1, lastSession: {}, current: null })
  })

  it('чужая версия схемы игнорируется', () => {
    localStorage.setItem(KEY, JSON.stringify({ version: 99, lastSession: { x: 1 } }))
    expect(loadState().lastSession).toEqual({})
  })

  it('недостающие поля добираются умолчаниями', () => {
    localStorage.setItem(KEY, JSON.stringify({ version: 1 }))
    expect(loadState()).toEqual({ version: 1, lastSession: {}, current: null })
  })

  it('clearState стирает запись', () => {
    saveState({ version: 1, lastSession: { a: 1 }, current: null })
    clearState()
    expect(loadState().lastSession).toEqual({})
  })
})
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `npm test -- storage`
Expected: FAIL, `Failed to resolve import "./storage.js"`.

- [ ] **Step 3: Реализовать `src/lib/storage.js`**

```js
const KEY = 'arvix.v1'

export const EMPTY_STATE = { version: 1, lastSession: {}, current: null }

function empty() {
  return { version: 1, lastSession: {}, current: null }
}

export function loadState() {
  try {
    const raw = localStorage.getItem(KEY)
    if (!raw) return empty()
    const parsed = JSON.parse(raw)
    if (!parsed || parsed.version !== 1) return empty()
    return {
      version: 1,
      lastSession: parsed.lastSession ?? {},
      current: parsed.current ?? null,
    }
  } catch {
    return empty()
  }
}

export function saveState(state) {
  try {
    localStorage.setItem(KEY, JSON.stringify(state))
  } catch {
    // Переполнена квота или приватный режим. Тренировка важнее записи —
    // молча продолжаем, экран остаётся рабочим.
  }
}

export function clearState() {
  try {
    localStorage.removeItem(KEY)
  } catch {
    // см. saveState
  }
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test -- storage`
Expected: PASS, 6 тестов.

- [ ] **Step 5: Удалить дымовой тест и закоммитить**

```bash
rm src/lib/smoke.test.js
git add -A
git commit -m "feat: хранилище прогресса в localStorage"
```

---

### Task 3: `lib/progression.js` — двойная прогрессия

**Files:**
- Create: `src/lib/progression.js`
- Test: `src/lib/progression.test.js`

**Interfaces:**
- Consumes: ничего
- Produces: `suggestWeight(lastSession, block, weightStep, currentWeight): number|null`
  - `lastSession`: `{weight: number, reps: Array<number|null>}` или `undefined`
  - `block`: `{sets: number, reps: [number, number]}`
  - `currentWeight`: `number|null`
  - Возвращает предлагаемый вес или `null`, если подсказки нет

Правило дословно из спеки, раздел 8: подсказка появляется тогда и только тогда, когда записаны все `n` подходов, минимум по повторам не ниже верхней границы диапазона, и текущий вес не ниже прошлого.

- [ ] **Step 1: Написать падающие тесты**

```js
// src/lib/progression.test.js
import { describe, it, expect } from 'vitest'
import { suggestWeight } from './progression.js'

const block = { sets: 3, reps: [5, 8] }

describe('suggestWeight', () => {
  it('все подходы на верхней границе — предлагает прибавку', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8, 8] }, block, 2.5, 60)).toBe(62.5)
  })

  it('повторов больше верхней границы — тоже прибавка', () => {
    expect(suggestWeight({ weight: 60, reps: [9, 8, 10] }, block, 2.5, 60)).toBe(62.5)
  })

  it('один подход недобрал — молчит', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8, 7] }, block, 2.5, 60)).toBeNull()
  })

  it('незакрытый подход — молчит', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8, null] }, block, 2.5, 60)).toBeNull()
  })

  it('подходов записано меньше планового числа — молчит', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8] }, block, 2.5, 60)).toBeNull()
  })

  it('вес сегодня ниже прошлого — молчит', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8, 8] }, block, 2.5, 55)).toBeNull()
  })

  it('вес сегодня ещё не выставлен — подсказка есть', () => {
    expect(suggestWeight({ weight: 60, reps: [8, 8, 8] }, block, 2.5, null)).toBe(62.5)
  })

  it('прошлой сессии нет — молчит', () => {
    expect(suggestWeight(undefined, block, 2.5, 60)).toBeNull()
  })

  it('шаг веса берётся из аргумента', () => {
    expect(suggestWeight({ weight: 100, reps: [12, 12], date: '' }, { sets: 2, reps: [10, 12] }, 5, 100)).toBe(105)
  })

  it('прошлый вес не записан — молчит', () => {
    expect(suggestWeight({ weight: null, reps: [8, 8, 8] }, block, 2.5, 60)).toBeNull()
  })
})
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `npm test -- progression`
Expected: FAIL, модуль не найден.

- [ ] **Step 3: Реализовать `src/lib/progression.js`**

```js
/**
 * Правило двойной прогрессии (спека, раздел 8).
 *
 * @param {{weight: number|null, reps: Array<number|null>}|undefined} lastSession
 * @param {{sets: number, reps: [number, number]}} block
 * @param {number} weightStep
 * @param {number|null} currentWeight
 * @returns {number|null} предлагаемый вес или null
 */
export function suggestWeight(lastSession, block, weightStep, currentWeight) {
  if (!lastSession) return null

  const { weight, reps } = lastSession
  if (typeof weight !== 'number') return null
  if (!Array.isArray(reps)) return null

  // Условие 1: записаны все плановые подходы
  if (reps.length !== block.sets) return null
  if (reps.some((r) => typeof r !== 'number')) return null

  // Условие 2: минимум по повторам не ниже верхней границы диапазона
  const max = block.reps[1]
  if (Math.min(...reps) < max) return null

  // Условие 3: сегодня вес не снижен сознательно
  if (currentWeight != null && currentWeight < weight) return null

  return weight + weightStep
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test -- progression`
Expected: PASS, 10 тестов.

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: правило двойной прогрессии"
```

---

### Task 4: `lib/timer.js` — таймер отдыха

**Files:**
- Create: `src/lib/timer.js`
- Test: `src/lib/timer.test.js`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `createTimer(seconds): Timer` где `Timer = {total, remaining, running, done}`
  - `startTimer(t): Timer`
  - `pauseTimer(t): Timer`
  - `tick(t): Timer`
  - `formatTime(seconds): string` — `"3:00"`

- [ ] **Step 1: Написать падающие тесты**

```js
// src/lib/timer.test.js
import { describe, it, expect } from 'vitest'
import { createTimer, startTimer, pauseTimer, tick, formatTime } from './timer.js'

describe('таймер', () => {
  it('создаётся остановленным на полном времени', () => {
    expect(createTimer(180)).toEqual({ total: 180, remaining: 180, running: false, done: false })
  })

  it('запуск включает отсчёт', () => {
    expect(startTimer(createTimer(60)).running).toBe(true)
  })

  it('тик уменьшает остаток на секунду', () => {
    const t = tick(startTimer(createTimer(60)))
    expect(t.remaining).toBe(59)
  })

  it('на паузе тик ничего не меняет', () => {
    const paused = pauseTimer(startTimer(createTimer(60)))
    expect(tick(paused)).toEqual(paused)
  })

  it('в нуле останавливается и помечается завершённым', () => {
    let t = startTimer(createTimer(2))
    t = tick(t)
    t = tick(t)
    expect(t).toMatchObject({ remaining: 0, running: false, done: true })
  })

  it('тик после нуля не уводит остаток в минус', () => {
    let t = startTimer(createTimer(1))
    t = tick(t)
    t = tick(t)
    expect(t.remaining).toBe(0)
  })

  it('пауза и повторный запуск сохраняют остаток', () => {
    let t = tick(startTimer(createTimer(60)))
    t = startTimer(pauseTimer(t))
    expect(t).toMatchObject({ remaining: 59, running: true })
  })

  it('форматирует время', () => {
    expect(formatTime(180)).toBe('3:00')
    expect(formatTime(45)).toBe('0:45')
    expect(formatTime(5)).toBe('0:05')
    expect(formatTime(0)).toBe('0:00')
    expect(formatTime(125)).toBe('2:05')
  })
})
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `npm test -- timer`
Expected: FAIL, модуль не найден.

- [ ] **Step 3: Реализовать `src/lib/timer.js`**

```js
export function createTimer(seconds) {
  return { total: seconds, remaining: seconds, running: false, done: false }
}

export function startTimer(t) {
  return { ...t, running: true, done: false }
}

export function pauseTimer(t) {
  return { ...t, running: false }
}

export function tick(t) {
  if (!t.running) return t
  const remaining = t.remaining - 1
  if (remaining <= 0) return { ...t, remaining: 0, running: false, done: true }
  return { ...t, remaining }
}

export function formatTime(seconds) {
  const m = Math.floor(seconds / 60)
  const s = seconds % 60
  return `${m}:${String(s).padStart(2, '0')}`
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test -- timer`
Expected: PASS, 8 тестов.

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: редьюсер таймера отдыха"
```

---

### Task 5: `lib/router.js` — хеш-роутинг

**Files:**
- Create: `src/lib/router.js`
- Test: `src/lib/router.test.js`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `parseHash(hash): Route` где `Route` — одно из `{screen:'days'}`, `{screen:'workout', dayId}`, `{screen:'exercise', exerciseId}`
  - `hashFor(route): string`
  - `navigate(route): void`
  - `subscribeRoute(fn): () => void` — возвращает отписку

- [ ] **Step 1: Написать падающие тесты**

```js
// src/lib/router.test.js
import { describe, it, expect } from 'vitest'
import { parseHash, hashFor } from './router.js'

describe('parseHash', () => {
  it('пустой хеш — список дней', () => {
    expect(parseHash('')).toEqual({ screen: 'days' })
    expect(parseHash('#')).toEqual({ screen: 'days' })
    expect(parseHash('#/')).toEqual({ screen: 'days' })
  })

  it('день', () => {
    expect(parseHash('#/day/thu')).toEqual({ screen: 'workout', dayId: 'thu' })
  })

  it('упражнение', () => {
    expect(parseHash('#/ex/back-squat')).toEqual({ screen: 'exercise', exerciseId: 'back-squat' })
  })

  it('мусор скатывается к списку дней', () => {
    expect(parseHash('#/чтототакое')).toEqual({ screen: 'days' })
    expect(parseHash('#/day')).toEqual({ screen: 'days' })
    expect(parseHash('#/ex/')).toEqual({ screen: 'days' })
  })

  it('undefined не роняет', () => {
    expect(parseHash(undefined)).toEqual({ screen: 'days' })
  })
})

describe('hashFor', () => {
  it('собирает хеш обратно', () => {
    expect(hashFor({ screen: 'days' })).toBe('#/')
    expect(hashFor({ screen: 'workout', dayId: 'tue' })).toBe('#/day/tue')
    expect(hashFor({ screen: 'exercise', exerciseId: 'pull-up' })).toBe('#/ex/pull-up')
  })

  it('парсинг и сборка обратимы', () => {
    for (const h of ['#/', '#/day/sun', '#/ex/leg-press']) {
      expect(hashFor(parseHash(h))).toBe(h)
    }
  })
})
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `npm test -- router`
Expected: FAIL, модуль не найден.

- [ ] **Step 3: Реализовать `src/lib/router.js`**

```js
export function parseHash(hash) {
  const clean = String(hash ?? '').replace(/^#\/?/, '')
  if (!clean) return { screen: 'days' }

  const [head, param] = clean.split('/')
  if (head === 'day' && param) return { screen: 'workout', dayId: param }
  if (head === 'ex' && param) return { screen: 'exercise', exerciseId: param }
  return { screen: 'days' }
}

export function hashFor(route) {
  if (route.screen === 'workout') return `#/day/${route.dayId}`
  if (route.screen === 'exercise') return `#/ex/${route.exerciseId}`
  return '#/'
}

export function navigate(route) {
  window.location.hash = hashFor(route)
}

export function subscribeRoute(fn) {
  const handler = () => fn(parseHash(window.location.hash))
  window.addEventListener('hashchange', handler)
  return () => window.removeEventListener('hashchange', handler)
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test -- router`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: хеш-роутинг без библиотеки"
```

---

### Task 6: `lib/workout.js` — состояние тренировки

**Files:**
- Create: `src/lib/workout.js`
- Test: `src/lib/workout.test.js`

**Interfaces:**
- Consumes: форма состояния из `lib/storage.js`
- Produces (все функции чистые, возвращают новое состояние):
  - `startWorkout(state, dayId, startedAt): State`
  - `setWeight(state, exerciseId, weight): State`
  - `closeSet(state, exerciseId, setIndex, sets, repsValue): State`
  - `clearSet(state, exerciseId, setIndex, sets): State`
  - `toggleAthletic(state, exerciseId): State`
  - `finishWorkout(state, date): State`

- [ ] **Step 1: Написать падающие тесты**

```js
// src/lib/workout.test.js
import { describe, it, expect } from 'vitest'
import {
  startWorkout, setWeight, closeSet, clearSet, toggleAthletic, finishWorkout,
} from './workout.js'

const empty = { version: 1, lastSession: {}, current: null }
const started = startWorkout(empty, 'thu', '2026-07-30T18:00:00.000Z')

describe('startWorkout', () => {
  it('заводит текущую тренировку', () => {
    expect(started.current).toEqual({
      dayId: 'thu',
      startedAt: '2026-07-30T18:00:00.000Z',
      reps: {},
      weights: {},
      athletic: {},
    })
  })

  it('поверх незавершённой тренировки начинает с чистого листа', () => {
    const dirty = closeSet(started, 'back-squat', 0, 3, 8)
    const again = startWorkout(dirty, 'tue', '2026-08-01T10:00:00.000Z')
    expect(again.current.reps).toEqual({})
    expect(again.current.dayId).toBe('tue')
  })

  it('не трогает прошлые сессии', () => {
    const withHistory = { ...empty, lastSession: { 'back-squat': { weight: 80, reps: [8], date: 'x' } } }
    expect(startWorkout(withHistory, 'thu', 'now').lastSession).toEqual(withHistory.lastSession)
  })
})

describe('closeSet', () => {
  it('пишет повторы в нужный индекс, остальные остаются пустыми', () => {
    const s = closeSet(started, 'back-squat', 1, 3, 8)
    expect(s.current.reps['back-squat']).toEqual([null, 8, null])
  })

  it('повторное закрытие перезаписывает значение', () => {
    let s = closeSet(started, 'back-squat', 0, 3, 8)
    s = closeSet(s, 'back-squat', 0, 3, 6)
    expect(s.current.reps['back-squat']).toEqual([6, null, null])
  })

  it('без начатой тренировки ничего не делает', () => {
    expect(closeSet(empty, 'back-squat', 0, 3, 8)).toEqual(empty)
  })
})

describe('clearSet', () => {
  it('возвращает подход в незакрытое состояние', () => {
    let s = closeSet(started, 'back-squat', 0, 3, 8)
    s = clearSet(s, 'back-squat', 0, 3)
    expect(s.current.reps['back-squat']).toEqual([null, null, null])
  })
})

describe('setWeight', () => {
  it('запоминает рабочий вес', () => {
    expect(setWeight(started, 'back-squat', 82.5).current.weights).toEqual({ 'back-squat': 82.5 })
  })
})

describe('toggleAthletic', () => {
  it('отмечает и снимает отметку', () => {
    let s = toggleAthletic(started, 'jump-rope')
    expect(s.current.athletic['jump-rope']).toBe(true)
    s = toggleAthletic(s, 'jump-rope')
    expect(s.current.athletic['jump-rope']).toBeUndefined()
  })
})

describe('finishWorkout', () => {
  it('переносит сделанное в историю и закрывает тренировку', () => {
    let s = closeSet(started, 'back-squat', 0, 3, 8)
    s = closeSet(s, 'back-squat', 1, 3, 8)
    s = closeSet(s, 'back-squat', 2, 3, 7)
    s = setWeight(s, 'back-squat', 80)
    const done = finishWorkout(s, '2026-07-30')

    expect(done.current).toBeNull()
    expect(done.lastSession['back-squat']).toEqual({
      weight: 80, reps: [8, 8, 7], date: '2026-07-30',
    })
  })

  it('упражнения без единого закрытого подхода в историю не попадают', () => {
    const s = closeSet(started, 'back-squat', 0, 3, 8)
    const done = finishWorkout(s, '2026-07-30')
    expect(Object.keys(done.lastSession)).toEqual(['back-squat'])
  })

  it('нетронутое упражнение не затирает прошлый результат', () => {
    const withHistory = {
      ...empty,
      lastSession: { 'bench-press': { weight: 60, reps: [8, 8, 8], date: '2026-07-23' } },
    }
    let s = startWorkout(withHistory, 'tue', 'now')
    s = closeSet(s, 'db-row', 0, 3, 10)
    const done = finishWorkout(s, '2026-07-30')
    expect(done.lastSession['bench-press']).toEqual({ weight: 60, reps: [8, 8, 8], date: '2026-07-23' })
  })

  it('без начатой тренировки ничего не делает', () => {
    expect(finishWorkout(empty, '2026-07-30')).toEqual(empty)
  })
})
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `npm test -- workout`
Expected: FAIL, модуль не найден.

- [ ] **Step 3: Реализовать `src/lib/workout.js`**

```js
export function startWorkout(state, dayId, startedAt) {
  return {
    ...state,
    current: { dayId, startedAt, reps: {}, weights: {}, athletic: {} },
  }
}

export function setWeight(state, exerciseId, weight) {
  if (!state.current) return state
  return {
    ...state,
    current: {
      ...state.current,
      weights: { ...state.current.weights, [exerciseId]: weight },
    },
  }
}

export function closeSet(state, exerciseId, setIndex, sets, repsValue) {
  if (!state.current) return state
  const existing = state.current.reps[exerciseId] ?? Array(sets).fill(null)
  const next = [...existing]
  next[setIndex] = repsValue
  return {
    ...state,
    current: {
      ...state.current,
      reps: { ...state.current.reps, [exerciseId]: next },
    },
  }
}

export function clearSet(state, exerciseId, setIndex, sets) {
  return closeSet(state, exerciseId, setIndex, sets, null)
}

export function toggleAthletic(state, exerciseId) {
  if (!state.current) return state
  const athletic = { ...state.current.athletic }
  if (athletic[exerciseId]) delete athletic[exerciseId]
  else athletic[exerciseId] = true
  return { ...state, current: { ...state.current, athletic } }
}

export function finishWorkout(state, date) {
  if (!state.current) return state

  const lastSession = { ...state.lastSession }
  for (const [exerciseId, reps] of Object.entries(state.current.reps)) {
    // Упражнение, к которому не притронулись, не должен затирать прошлый результат
    if (reps.every((r) => r === null || r === undefined)) continue
    lastSession[exerciseId] = {
      weight: state.current.weights[exerciseId] ?? null,
      reps,
      date,
    }
  }

  return { version: 1, lastSession, current: null }
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test -- workout`
Expected: PASS, 12 тестов.

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: переходы состояния тренировки"
```

---

### Task 7: Контент — библиотека упражнений и программа

**Files:**
- Create: `src/data/exercises.json`
- Create: `src/data/program.json`
- Create: `src/lib/program.js`
- Test: `src/data/data.test.js`
- Test: `src/lib/program.test.js`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `exercises.json` — объект `{[id]: Exercise}`, 22 записи
  - `program.json` — `{title, days: Day[]}`, 3 дня
  - `todayDayId(date): string|null`
  - `findDay(program, dayId): Day|null`

- [ ] **Step 1: Написать `src/data/program.json` целиком**

Скопировано из спеки, раздел 6, без изменений.

```json
{
  "title": "Full Body 3×/нед",
  "days": [
    {
      "id": "tue",
      "weekday": "Вторник",
      "accent": "Грудь и спина",
      "blocks": [
        { "exercise": "bench-press", "sets": 3, "reps": [5, 8], "rir": 2, "rest": 180, "tempo": "2-0-1" },
        { "exercise": "db-row", "sets": 3, "reps": [8, 10], "rir": 2, "rest": 120 },
        { "exercise": "pull-up", "sets": 3, "reps": [6, 10], "rir": 2, "rest": 150 },
        { "exercise": "incline-db-press", "sets": 2, "reps": [10, 12], "rir": 1, "rest": 120 },
        { "exercise": "bulgarian-split-squat", "sets": 2, "reps": [8, 10], "rir": 2, "rest": 120 },
        { "exercise": "leg-curl", "sets": 2, "reps": [10, 12], "rir": 1, "rest": 90 }
      ],
      "athletic": [
        { "exercise": "bosu-balance", "format": "3 × 30 с на ногу", "rest": 30 },
        { "exercise": "band-rotation", "format": "3 × 10 на сторону", "rest": 30 },
        { "exercise": "jump-rope", "format": "3 × 45 с", "rest": 45 }
      ]
    },
    {
      "id": "thu",
      "weekday": "Четверг",
      "accent": "Ноги",
      "blocks": [
        { "exercise": "back-squat", "sets": 3, "reps": [5, 8], "rir": 2, "rest": 180, "tempo": "2-0-1" },
        { "exercise": "db-rdl", "sets": 3, "reps": [8, 10], "rir": 2, "rest": 150, "tempo": "3-0-1" },
        { "exercise": "leg-press", "sets": 2, "reps": [10, 12], "rir": 1, "rest": 120 },
        { "exercise": "pull-up", "sets": 3, "reps": [6, 10], "rir": 2, "rest": 120 },
        { "exercise": "db-bench-press", "sets": 3, "reps": [8, 10], "rir": 2, "rest": 120 },
        { "exercise": "lateral-raise", "sets": 2, "reps": [12, 15], "rir": 1, "rest": 60 }
      ],
      "athletic": [
        { "exercise": "agility-ladder", "format": "3 × 30 с", "rest": 30 },
        { "exercise": "medball-slam", "format": "3 × 8", "rest": 45 },
        { "exercise": "jump-rope", "format": "3 × 45 с", "rest": 45 }
      ]
    },
    {
      "id": "sun",
      "weekday": "Воскресенье",
      "accent": "Плечи и руки",
      "blocks": [
        { "exercise": "seated-db-press", "sets": 3, "reps": [6, 10], "rir": 2, "rest": 150 },
        { "exercise": "leg-press", "sets": 3, "reps": [10, 12], "rir": 2, "rest": 120 },
        { "exercise": "seated-row", "sets": 3, "reps": [8, 10], "rir": 2, "rest": 120 },
        { "exercise": "lateral-raise", "sets": 3, "reps": [12, 15], "rir": 1, "rest": 60 },
        { "exercise": "db-curl", "sets": 2, "reps": [10, 12], "rir": 1, "rest": 60 },
        { "exercise": "triceps-pushdown", "sets": 2, "reps": [10, 12], "rir": 1, "rest": 60 }
      ],
      "athletic": [
        { "exercise": "kettlebell-swing", "format": "3 × 12", "rest": 45 },
        { "exercise": "squat-jump", "format": "3 × 5", "rest": 60 },
        { "exercise": "jump-rope", "format": "3 × 45 с", "rest": 45 }
      ]
    }
  ]
}
```

- [ ] **Step 2: Написать тест-валидатор данных**

Этот тест — и проверка структуры, и планка качества текстов. Он должен быть написан **до** библиотеки упражнений и упасть на ней.

```js
// src/data/data.test.js
import { describe, it, expect } from 'vitest'
import exercises from './exercises.json'
import program from './program.json'

const ALL_BLOCKS = program.days.flatMap((d) => d.blocks)
const ALL_ATHLETIC = program.days.flatMap((d) => d.athletic)

describe('программа', () => {
  it('содержит ровно три дня с ожидаемыми акцентами', () => {
    expect(program.days.map((d) => [d.id, d.accent])).toEqual([
      ['tue', 'Грудь и спина'],
      ['thu', 'Ноги'],
      ['sun', 'Плечи и руки'],
    ])
  })

  it('у каждого силового блока заполнены обязательные поля', () => {
    for (const b of ALL_BLOCKS) {
      expect(typeof b.exercise, `${b.exercise}: id`).toBe('string')
      expect(b.sets, `${b.exercise}: sets`).toBeGreaterThan(0)
      expect(b.reps, `${b.exercise}: reps`).toHaveLength(2)
      expect(b.reps[0], `${b.exercise}: диапазон`).toBeLessThanOrEqual(b.reps[1])
      expect(typeof b.rir, `${b.exercise}: rir`).toBe('number')
      expect(b.rest, `${b.exercise}: rest`).toBeGreaterThan(0)
    }
  })

  it('у каждого атлетического блока есть формат и отдых', () => {
    for (const a of ALL_ATHLETIC) {
      expect(typeof a.format, `${a.exercise}: format`).toBe('string')
      expect(a.rest, `${a.exercise}: rest`).toBeGreaterThan(0)
    }
  })

  it('каждое упражнение из программы есть в библиотеке', () => {
    const missing = [...ALL_BLOCKS, ...ALL_ATHLETIC]
      .map((b) => b.exercise)
      .filter((id) => !exercises[id])
    expect(missing).toEqual([])
  })
})

describe('библиотека упражнений', () => {
  it('содержит 22 упражнения', () => {
    expect(Object.keys(exercises)).toHaveLength(22)
  })

  it('в библиотеке нет упражнений, которых нет в программе', () => {
    const used = new Set([...ALL_BLOCKS, ...ALL_ATHLETIC].map((b) => b.exercise))
    const unused = Object.keys(exercises).filter((id) => !used.has(id))
    expect(unused).toEqual([])
  })

  it('у каждого упражнения заполнены все поля', () => {
    for (const [id, ex] of Object.entries(exercises)) {
      expect(ex.name, `${id}: name`).toBeTruthy()
      expect(ex.equipment, `${id}: equipment`).toBeTruthy()
      expect(ex.primary.length, `${id}: primary`).toBeGreaterThan(0)
      expect(Array.isArray(ex.secondary), `${id}: secondary`).toBe(true)
      expect(typeof ex.weightStep, `${id}: weightStep`).toBe('number')
    }
  })

  it('техника описана не поверхностно', () => {
    for (const [id, ex] of Object.entries(exercises)) {
      expect(ex.steps.length, `${id}: шагов техники`).toBeGreaterThanOrEqual(3)
      expect(ex.mistakes.length, `${id}: частых ошибок`).toBeGreaterThanOrEqual(2)
      expect(ex.tip.length, `${id}: подсказка`).toBeGreaterThan(20)
      for (const step of ex.steps) {
        expect(step.length, `${id}: шаг «${step}»`).toBeGreaterThan(15)
      }
    }
  })

  it('идентификаторы пригодны как имена файлов', () => {
    for (const id of Object.keys(exercises)) {
      expect(id, `${id}`).toMatch(/^[a-z0-9-]+$/)
    }
  })
})
```

- [ ] **Step 3: Прогнать и убедиться, что падает**

Run: `npm test -- data`
Expected: FAIL, `Cannot find module './exercises.json'`.

- [ ] **Step 4: Написать `src/data/exercises.json`**

Структурные поля заданы таблицей ниже полностью — их переписать один в один. Прозаические поля (`steps`, `mistakes`, `tip`) пишутся по образцу двух эталонных записей и должны пройти тест из шага 2: минимум 3 шага техники по 15+ символов, минимум 2 частые ошибки, подсказка длиннее 20 символов.

Эталон формата — две записи, силовая и атлетическая:

```json
{
  "back-squat": {
    "name": "Приседания со штангой",
    "equipment": "Штанга, стойки",
    "primary": ["Квадрицепс", "Ягодичные"],
    "secondary": ["Бицепс бедра", "Разгибатели спины"],
    "steps": [
      "Штанга на верхе трапеций, хват чуть шире плеч, локти под грифом.",
      "Стопы на ширине плеч, носки развёрнуты на 15–30 градусов.",
      "Вдох, задержка дыхания, напряги пресс — рёбра вниз, таз подвёрнут.",
      "Садись, разводя колени по линии стоп, до параллели бедра с полом.",
      "Встань, отталкиваясь всей стопой, выдох в верхней точке."
    ],
    "mistakes": [
      "Колени сводятся внутрь на подъёме",
      "Круглится поясница в нижней точке",
      "Отрыв пяток от пола"
    ],
    "tip": "Если пятки отрываются — не хватает подвижности голеностопа. Подложи блины под пятки или возьми штангетки.",
    "weightStep": 2.5
  },
  "jump-rope": {
    "name": "Скакалка",
    "equipment": "Скакалка",
    "primary": ["Икроножные", "Сердечно-сосудистая система"],
    "secondary": ["Предплечья", "Стопа"],
    "steps": [
      "Локти прижаты к корпусу, вращение идёт от кистей, а не от плеч.",
      "Прыжки низкие, отрыв от пола на два-три сантиметра.",
      "Приземление на переднюю часть стопы, пятка едва касается пола.",
      "Дыхание ровное, темп держится постоянным весь подход."
    ],
    "mistakes": [
      "Прыжки слишком высокие — быстро забиваются икры",
      "Вращение всей рукой вместо кисти",
      "Жёсткое приземление на пятку"
    ],
    "tip": "Если сбиваешься — начни с интервалов по 20 секунд и наращивай, техника важнее непрерывности.",
    "weightStep": 0
  }
}
```

Полный список из 22 записей. Колонки `name`, `equipment`, `primary`, `secondary`, `weightStep` переносятся дословно.

| id | name | equipment | primary | secondary | weightStep |
|---|---|---|---|---|---|
| `bench-press` | Жим штанги лёжа | Штанга, горизонтальная скамья | Грудные | Трицепс, Передняя дельта | 2.5 |
| `db-row` | Тяга гантели в упоре | Гантель, скамья | Широчайшие | Бицепс, Задняя дельта | 2 |
| `pull-up` | Подтягивания | Турник | Широчайшие | Бицепс, Круглые мышцы спины | 2.5 |
| `incline-db-press` | Жим гантелей на наклонной | Гантели, наклонная скамья | Верх грудных | Передняя дельта, Трицепс | 2 |
| `bulgarian-split-squat` | Болгарский сплит-присед | Гантели, скамья | Квадрицепс, Ягодичные | Бицепс бедра, Мышцы стабилизаторы | 2 |
| `leg-curl` | Сгибание ног лёжа | Тренажёр для сгибания ног | Бицепс бедра | Икроножные | 2.5 |
| `back-squat` | Приседания со штангой | Штанга, стойки | Квадрицепс, Ягодичные | Бицепс бедра, Разгибатели спины | 2.5 |
| `db-rdl` | Румынская тяга с гантелями | Гантели | Бицепс бедра, Ягодичные | Разгибатели спины, Предплечья | 2 |
| `leg-press` | Жим ногами | Тренажёр для жима ногами | Квадрицепс | Ягодичные, Бицепс бедра | 5 |
| `db-bench-press` | Жим гантелей лёжа | Гантели, горизонтальная скамья | Грудные | Трицепс, Передняя дельта | 2 |
| `lateral-raise` | Махи гантелями в стороны | Гантели | Средняя дельта | Трапеции, Задняя дельта | 2 |
| `seated-db-press` | Жим гантелей сидя | Гантели, скамья со спинкой | Передняя дельта, Средняя дельта | Трицепс, Верх грудных | 2 |
| `seated-row` | Тяга горизонтального блока | Блочный тренажёр | Широчайшие, Ромбовидные | Бицепс, Задняя дельта | 2.5 |
| `db-curl` | Сгибания на бицепс с гантелями | Гантели | Бицепс | Плечелучевая, Предплечья | 2 |
| `triceps-pushdown` | Разгибания на трицепс на блоке | Блочный тренажёр, канат | Трицепс | Предплечья | 2.5 |
| `bosu-balance` | Баланс на BOSU | BOSU | Мышцы стабилизаторы, Стопа | Квадрицепс, Ягодичные | 0 |
| `band-rotation` | Ротации с резинкой | Резиновая лента | Косые мышцы живота | Пресс, Задняя дельта | 0 |
| `jump-rope` | Скакалка | Скакалка | Икроножные, Сердечно-сосудистая система | Предплечья, Стопа | 0 |
| `agility-ladder` | Координационная лестница | Координационная лестница | Координация, Икроножные | Квадрицепс, Стопа | 0 |
| `medball-slam` | Медбол-слэм | Медбол | Пресс, Широчайшие | Плечи, Ягодичные | 0 |
| `kettlebell-swing` | Свинги с гирей | Гиря | Ягодичные, Бицепс бедра | Разгибатели спины, Пресс | 0 |
| `squat-jump` | Выпрыгивания из полуприседа | Без оборудования | Квадрицепс, Ягодичные | Икроножные, Бицепс бедра | 0 |

`weightStep: 0` у атлетических упражнений — они не участвуют в двойной прогрессии, поле остаётся ради единой формы записи.

- [ ] **Step 5: Прогнать тесты данных**

Run: `npm test -- data`
Expected: PASS, 8 тестов. Если падает «техника описана не поверхностно» — дописать шаги в указанное упражнение, сообщение об ошибке называет id.

- [ ] **Step 6: Написать тесты `lib/program.js`**

```js
// src/lib/program.test.js
import { describe, it, expect } from 'vitest'
import { todayDayId, findDay } from './program.js'
import program from '../data/program.json'

describe('todayDayId', () => {
  it('вторник, четверг и воскресенье — тренировочные', () => {
    expect(todayDayId(new Date('2026-07-28T10:00:00'))).toBe('tue')
    expect(todayDayId(new Date('2026-07-30T10:00:00'))).toBe('thu')
    expect(todayDayId(new Date('2026-08-02T10:00:00'))).toBe('sun')
  })

  it('в остальные дни возвращает null', () => {
    expect(todayDayId(new Date('2026-07-29T10:00:00'))).toBeNull()
    expect(todayDayId(new Date('2026-07-31T10:00:00'))).toBeNull()
  })
})

describe('findDay', () => {
  it('находит день по id', () => {
    expect(findDay(program, 'thu').accent).toBe('Ноги')
  })

  it('для неизвестного id возвращает null', () => {
    expect(findDay(program, 'mon')).toBeNull()
  })
})
```

- [ ] **Step 7: Прогнать и убедиться, что падает**

Run: `npm test -- program`
Expected: FAIL, модуль не найден.

- [ ] **Step 8: Реализовать `src/lib/program.js`**

```js
// Индексы Date#getDay: 0 — воскресенье, 2 — вторник, 4 — четверг
const WEEKDAY_TO_DAY = { 0: 'sun', 2: 'tue', 4: 'thu' }

export function todayDayId(date) {
  return WEEKDAY_TO_DAY[date.getDay()] ?? null
}

export function findDay(program, dayId) {
  return program.days.find((d) => d.id === dayId) ?? null
}
```

- [ ] **Step 9: Прогнать все тесты**

Run: `npm test`
Expected: PASS, все файлы зелёные.

- [ ] **Step 10: Коммит**

```bash
git add -A
git commit -m "feat: библиотека из 22 упражнений и программа на три дня"
```

---

### Task 8: Главный экран

**Files:**
- Create: `src/components/DayList.jsx`, `src/components/DayList.module.css`
- Modify: `src/App.jsx` (полная замена заглушки)

**Interfaces:**
- Consumes: `parseHash`, `subscribeRoute`, `navigate` из `lib/router.js`; `todayDayId`, `findDay` из `lib/program.js`; `program.json`
- Produces:
  - `<DayList program todayId onPick />` — `onPick(dayId)`
  - `App` с роутингом: `days` → `DayList`, остальные экраны пока заглушки

- [ ] **Step 1: Написать `src/components/DayList.jsx`**

```jsx
import styles from './DayList.module.css'

export default function DayList({ program, todayId, onPick }) {
  return (
    <div className={styles.screen}>
      <header className={styles.header}>
        <h1 className={styles.title}>Arvix</h1>
        <p className={styles.subtitle}>{program.title}</p>
      </header>

      <ul className={styles.list}>
        {program.days.map((day) => {
          const isToday = day.id === todayId
          return (
            <li key={day.id}>
              <button
                className={isToday ? `${styles.card} ${styles.today}` : styles.card}
                onClick={() => onPick(day.id)}
              >
                <span className={styles.weekday}>{day.weekday}</span>
                <span className={styles.accent}>{day.accent}</span>
                <span className={styles.count}>
                  {day.blocks.length} упражнений · финишер
                </span>
                {isToday && <span className={styles.badge}>сегодня</span>}
              </button>
            </li>
          )
        })}
      </ul>
    </div>
  )
}
```

- [ ] **Step 2: Написать `src/components/DayList.module.css`**

```css
.screen {
  max-width: 560px;
  margin: 0 auto;
  padding: 24px 16px calc(24px + env(safe-area-inset-bottom));
}

.header { margin-bottom: 24px; }

.title {
  margin: 0;
  font-size: 28px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.subtitle {
  margin: 4px 0 0;
  color: var(--text-dim);
  font-size: 15px;
}

.list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: grid;
  gap: 12px;
}

.card {
  position: relative;
  display: grid;
  gap: 4px;
  width: 100%;
  min-height: calc(var(--tap) + 32px);
  padding: 18px;
  text-align: left;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  opacity: 0.62;
  transition: opacity 0.15s ease;
}

.card:active { opacity: 1; }

.today {
  opacity: 1;
  border-color: var(--accent);
}

.weekday {
  font-size: 20px;
  font-weight: 600;
}

.accent {
  font-size: 15px;
  color: var(--accent);
}

.count {
  font-size: 13px;
  color: var(--text-dim);
}

.badge {
  position: absolute;
  top: 14px;
  right: 14px;
  padding: 4px 10px;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent-ink);
  background: var(--accent);
  border-radius: 999px;
}
```

- [ ] **Step 3: Переписать `src/App.jsx`**

```jsx
import { useEffect, useState } from 'react'
import { parseHash, subscribeRoute, navigate } from './lib/router.js'
import { todayDayId } from './lib/program.js'
import program from './data/program.json'
import DayList from './components/DayList.jsx'

export default function App() {
  const [route, setRoute] = useState(() => parseHash(window.location.hash))

  useEffect(() => subscribeRoute(setRoute), [])

  if (route.screen === 'days') {
    return (
      <DayList
        program={program}
        todayId={todayDayId(new Date())}
        onPick={(dayId) => navigate({ screen: 'workout', dayId })}
      />
    )
  }

  return <p style={{ padding: 24 }}>Экран в работе: {route.screen}</p>
}
```

- [ ] **Step 4: Проверить в браузере**

Run: `npm run dev`
Expected: три карточки, сегодняшний день подсвечен зелёной рамкой и меткой «сегодня» (если сегодня вторник, четверг или воскресенье). Тап по карточке меняет адрес на `#/day/<id>` и показывает заглушку. Кнопка «назад» в браузере возвращает к списку.

- [ ] **Step 5: Прогнать тесты и собрать**

Run: `npm test && npm run build`
Expected: тесты зелёные, сборка проходит.

- [ ] **Step 6: Коммит**

```bash
git add -A
git commit -m "feat: главный экран со списком дней"
```

---

### Task 9: Гифка с заглушкой и карточка техники

**Files:**
- Create: `src/components/ExerciseGif.jsx`, `src/components/ExerciseGif.module.css`
- Create: `src/components/ExerciseDetail.jsx`, `src/components/ExerciseDetail.module.css`
- Create: `public/gifs/.gitkeep`
- Modify: `src/App.jsx` (ветка `exercise`)

**Interfaces:**
- Consumes: `exercises.json`
- Produces:
  - `<ExerciseGif id name size />` — `size` одно из `'row' | 'detail'`
  - `<ExerciseDetail exercise onBack />`

- [ ] **Step 1: Написать `src/components/ExerciseGif.jsx`**

Отсутствие файла — штатная ситуация: гифки добавляются постепенно. Первая буква названия на плашке даёт хоть какое-то опознание.

```jsx
import { useState } from 'react'
import styles from './ExerciseGif.module.css'

export default function ExerciseGif({ id, name, size = 'row' }) {
  const [failed, setFailed] = useState(false)
  const className = size === 'detail' ? styles.detail : styles.row

  if (failed) {
    return (
      <div className={`${className} ${styles.placeholder}`} role="img" aria-label={name}>
        {name.slice(0, 1)}
      </div>
    )
  }

  return (
    <img
      className={className}
      src={`${import.meta.env.BASE_URL}gifs/${id}.gif`}
      alt={name}
      loading="lazy"
      onError={() => setFailed(true)}
    />
  )
}
```

- [ ] **Step 2: Написать `src/components/ExerciseGif.module.css`**

```css
.row {
  width: 84px;
  height: 84px;
  flex: none;
  object-fit: cover;
  border-radius: 10px;
  background: var(--surface-2);
}

.detail {
  width: 100%;
  max-height: 320px;
  object-fit: contain;
  border-radius: var(--radius);
  background: var(--surface-2);
}

.placeholder {
  display: grid;
  place-items: center;
  color: var(--text-dim);
  font-size: 28px;
  font-weight: 600;
  text-transform: uppercase;
}
```

- [ ] **Step 3: Написать `src/components/ExerciseDetail.jsx`**

```jsx
import ExerciseGif from './ExerciseGif.jsx'
import styles from './ExerciseDetail.module.css'

export default function ExerciseDetail({ exercise, exerciseId, onBack }) {
  return (
    <div className={styles.screen}>
      <button className={styles.back} onClick={onBack}>← Назад</button>

      <ExerciseGif id={exerciseId} name={exercise.name} size="detail" />

      <h1 className={styles.title}>{exercise.name}</h1>
      <p className={styles.equipment}>{exercise.equipment}</p>

      <section className={styles.section}>
        <h2 className={styles.heading}>Как делать</h2>
        <ol className={styles.steps}>
          {exercise.steps.map((step, i) => <li key={i}>{step}</li>)}
        </ol>
      </section>

      <section className={styles.section}>
        <h2 className={styles.heading}>Частые ошибки</h2>
        <ul className={styles.mistakes}>
          {exercise.mistakes.map((m, i) => <li key={i}>{m}</li>)}
        </ul>
      </section>

      <section className={styles.section}>
        <h2 className={styles.heading}>Работают мышцы</h2>
        <p className={styles.muscles}>
          <strong>{exercise.primary.join(', ')}</strong>
          {exercise.secondary.length > 0 && <> · {exercise.secondary.join(', ')}</>}
        </p>
      </section>

      <p className={styles.tip}>{exercise.tip}</p>
    </div>
  )
}
```

- [ ] **Step 4: Написать `src/components/ExerciseDetail.module.css`**

```css
.screen {
  max-width: 560px;
  margin: 0 auto;
  padding: 16px 16px calc(32px + env(safe-area-inset-bottom));
}

.back {
  min-height: var(--tap);
  margin-bottom: 8px;
  padding: 0 4px;
  color: var(--text-dim);
  font-size: 16px;
}

.title {
  margin: 16px 0 2px;
  font-size: 24px;
}

.equipment {
  margin: 0;
  color: var(--text-dim);
  font-size: 14px;
}

.section { margin-top: 28px; }

.heading {
  margin: 0 0 10px;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent);
}

.steps, .mistakes {
  margin: 0;
  padding-left: 20px;
  display: grid;
  gap: 8px;
  line-height: 1.5;
}

.mistakes { color: var(--danger); }

.muscles { margin: 0; line-height: 1.5; }

.tip {
  margin-top: 28px;
  padding: 14px 16px;
  background: var(--surface);
  border-left: 3px solid var(--accent);
  border-radius: 0 var(--radius) var(--radius) 0;
  line-height: 1.5;
}
```

- [ ] **Step 5: Подключить экран в `src/App.jsx`**

Добавить импорты и ветку перед финальным `return`:

```jsx
import exercises from './data/exercises.json'
import ExerciseDetail from './components/ExerciseDetail.jsx'
```

```jsx
  if (route.screen === 'exercise') {
    const exercise = exercises[route.exerciseId]
    if (!exercise) {
      navigate({ screen: 'days' })
      return null
    }
    return (
      <ExerciseDetail
        exercise={exercise}
        exerciseId={route.exerciseId}
        onBack={() => window.history.back()}
      />
    )
  }
```

- [ ] **Step 6: Создать `public/gifs/.gitkeep`**

```bash
mkdir -p public/gifs && touch public/gifs/.gitkeep
```

- [ ] **Step 7: Проверить в браузере**

Run: `npm run dev`, открыть `#/ex/back-squat`
Expected: карточка техники, вместо гифки — тёмная плашка с буквой «П» (файла ещё нет, падать не должно). Шаги, ошибки и мышцы на месте. «Назад» возвращает на список дней.

- [ ] **Step 8: Прогнать тесты, собрать, закоммитить**

```bash
npm test && npm run build
git add -A
git commit -m "feat: карточка техники упражнения"
```

---

### Task 10: Отметка подходов и строка упражнения

**Files:**
- Create: `src/components/SetTracker.jsx`, `src/components/SetTracker.module.css`
- Create: `src/components/ExerciseRow.jsx`, `src/components/ExerciseRow.module.css`

**Interfaces:**
- Consumes: `suggestWeight` из `lib/progression.js`; `<ExerciseGif />`
- Produces:
  - `<SetTracker sets reps repRange onClose onEdit onClear />`
    - `onClose(setIndex, repsValue)` — закрыть подход
    - `onEdit(setIndex, repsValue)` — изменить число повторов в закрытом подходе
    - `onClear(setIndex)` — вернуть подход в незакрытое состояние (промах пальцем)
  - `<ExerciseRow block exercise exerciseId reps weight lastSession onWeight onClose onEdit onClear onOpen />`

- [ ] **Step 1: Написать `src/components/SetTracker.jsx`**

Тап по пустому кружку закрывает подход на верхней границе диапазона — это один тап в хорошем случае. Тап по закрытому открывает степпер, где можно поправить число.

```jsx
import { useState } from 'react'
import styles from './SetTracker.module.css'

export default function SetTracker({ sets, reps, repRange, onClose, onEdit, onClear }) {
  const [editing, setEditing] = useState(null)
  const [min, max] = repRange
  const values = reps ?? Array(sets).fill(null)

  function handleTap(index) {
    if (values[index] == null) onClose(index, max)
    else setEditing(index)
  }

  const editValue = editing == null ? null : (values[editing] ?? max)

  return (
    <div className={styles.wrap}>
      <div className={styles.row}>
        {Array.from({ length: sets }, (_, i) => (
          <button
            key={i}
            className={values[i] == null ? styles.circle : `${styles.circle} ${styles.done}`}
            onClick={() => handleTap(i)}
            aria-label={values[i] == null ? `Подход ${i + 1}, не сделан` : `Подход ${i + 1}, ${values[i]} повторений`}
          >
            {values[i] ?? ''}
          </button>
        ))}
      </div>

      {editing != null && (
        <div className={styles.stepper}>
          <button
            className={styles.stepBtn}
            onClick={() => onEdit(editing, Math.max(1, editValue - 1))}
            aria-label="Меньше повторений"
          >−</button>

          <span className={styles.stepValue}>{editValue}</span>

          <button
            className={styles.stepBtn}
            onClick={() => onEdit(editing, editValue + 1)}
            aria-label="Больше повторений"
          >+</button>

          <button className={styles.stepDone} onClick={() => setEditing(null)}>Готово</button>

          <button
            className={styles.stepClear}
            onClick={() => { onClear(editing); setEditing(null) }}
          >Сбросить</button>

          <span className={styles.stepHint}>план {min}–{max}</span>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Написать `src/components/SetTracker.module.css`**

```css
.wrap { display: grid; gap: 10px; }

.row { display: flex; gap: 10px; flex-wrap: wrap; }

.circle {
  width: var(--tap);
  height: var(--tap);
  display: grid;
  place-items: center;
  font-size: 18px;
  font-weight: 600;
  color: var(--text-dim);
  background: var(--surface-2);
  border: 2px solid var(--line);
  border-radius: 50%;
}

.done {
  color: var(--accent-ink);
  background: var(--accent);
  border-color: var(--accent);
}

.stepper {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10px;
  padding: 10px;
  background: var(--surface-2);
  border-radius: var(--radius);
}

.stepBtn {
  width: var(--tap);
  height: var(--tap);
  font-size: 26px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 10px;
}

.stepValue {
  min-width: 40px;
  text-align: center;
  font-size: 22px;
  font-weight: 600;
}

.stepDone {
  min-height: var(--tap);
  padding: 0 18px;
  color: var(--accent-ink);
  background: var(--accent);
  border-radius: 10px;
  font-weight: 600;
}

.stepClear {
  min-height: var(--tap);
  padding: 0 14px;
  color: var(--text-dim);
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 10px;
}

.stepHint {
  width: 100%;
  font-size: 13px;
  color: var(--text-dim);
}
```

- [ ] **Step 3: Написать `src/components/ExerciseRow.jsx`**

```jsx
import ExerciseGif from './ExerciseGif.jsx'
import SetTracker from './SetTracker.jsx'
import { formatTime } from '../lib/timer.js'
import styles from './ExerciseRow.module.css'

export default function ExerciseRow({
  block, exercise, exerciseId, reps, weight, lastSession,
  onWeight, onClose, onEdit, onClear, onOpen,
}) {
  const [min, max] = block.reps
  const params = [
    `${block.sets} × ${min}–${max}`,
    `RIR ${block.rir}`,
    `отдых ${formatTime(block.rest)}`,
    block.tempo ? `темп ${block.tempo}` : null,
  ].filter(Boolean).join(' · ')

  return (
    <article className={styles.row}>
      <div className={styles.head}>
        <button className={styles.gifBtn} onClick={onOpen} aria-label={`Техника: ${exercise.name}`}>
          <ExerciseGif id={exerciseId} name={exercise.name} />
        </button>

        <div className={styles.meta}>
          <button className={styles.name} onClick={onOpen}>{exercise.name}</button>
          <p className={styles.params}>{params}</p>
        </div>
      </div>

      <div className={styles.weight}>
        <label className={styles.weightLabel} htmlFor={`w-${exerciseId}`}>Вес, кг</label>
        <input
          id={`w-${exerciseId}`}
          className={styles.weightInput}
          type="number"
          inputMode="decimal"
          step="0.5"
          value={weight ?? ''}
          placeholder="—"
          onChange={(e) => onWeight(e.target.value === '' ? null : Number(e.target.value))}
        />
        {lastSession?.weight != null && (
          <span className={styles.hint}>в прошлый раз: {lastSession.weight} кг</span>
        )}
      </div>

      <SetTracker
        sets={block.sets}
        reps={reps}
        repRange={block.reps}
        onClose={onClose}
        onEdit={onEdit}
        onClear={onClear}
      />
    </article>
  )
}
```

- [ ] **Step 4: Написать `src/components/ExerciseRow.module.css`**

```css
.row {
  display: grid;
  gap: 14px;
  padding: 16px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
}

.head { display: flex; gap: 14px; align-items: flex-start; }

.gifBtn { padding: 0; flex: none; }

.meta { display: grid; gap: 4px; min-width: 0; }

.name {
  padding: 0;
  text-align: left;
  font-size: 17px;
  font-weight: 600;
  line-height: 1.25;
}

.params {
  margin: 0;
  color: var(--text-dim);
  font-size: 13px;
  line-height: 1.4;
}

.weight { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }

.weightLabel { font-size: 14px; color: var(--text-dim); }

.weightInput {
  width: 96px;
  height: var(--tap);
  padding: 0 12px;
  font: inherit;
  font-size: 18px;
  font-weight: 600;
  color: var(--text);
  background: var(--surface-2);
  border: 1px solid var(--line);
  border-radius: 10px;
}

.hint { font-size: 13px; color: var(--text-dim); }
```

- [ ] **Step 5: Прогнать тесты и собрать**

Run: `npm test && npm run build`
Expected: тесты зелёные, сборка проходит. Визуально компоненты появятся в следующей задаче — здесь проверяется только то, что ничего не сломано.

- [ ] **Step 6: Коммит**

```bash
git add -A
git commit -m "feat: строка упражнения и отметка подходов"
```

---

### Task 11: Таймер отдыха

**Files:**
- Create: `src/hooks/useRestTimer.js`
- Create: `src/components/RestTimer.jsx`, `src/components/RestTimer.module.css`

**Interfaces:**
- Consumes: `createTimer`, `startTimer`, `pauseTimer`, `tick`, `formatTime` из `lib/timer.js`
- Produces:
  - `useRestTimer(): {timer, startRest(seconds), pauseRest(), resumeRest(), dismissRest()}`
  - `<RestTimer timer onPause onResume onDismiss />` — возвращает `null`, когда таймера нет

- [ ] **Step 1: Написать `src/hooks/useRestTimer.js`**

```js
import { useEffect, useState } from 'react'
import { createTimer, startTimer, pauseTimer, tick } from '../lib/timer.js'

export function useRestTimer() {
  const [timer, setTimer] = useState(null)
  const running = timer?.running ?? false
  const done = timer?.done ?? false

  useEffect(() => {
    if (!running) return
    const id = setInterval(() => setTimer((t) => (t ? tick(t) : t)), 1000)
    return () => clearInterval(id)
  }, [running])

  useEffect(() => {
    if (done) navigator.vibrate?.([200, 100, 200])
  }, [done])

  return {
    timer,
    startRest: (seconds) => setTimer(startTimer(createTimer(seconds))),
    pauseRest: () => setTimer((t) => (t ? pauseTimer(t) : t)),
    resumeRest: () => setTimer((t) => (t ? startTimer(t) : t)),
    dismissRest: () => setTimer(null),
  }
}
```

- [ ] **Step 2: Написать `src/components/RestTimer.jsx`**

```jsx
import { formatTime } from '../lib/timer.js'
import styles from './RestTimer.module.css'

export default function RestTimer({ timer, onPause, onResume, onDismiss }) {
  if (!timer) return null

  const progress = timer.total > 0 ? 1 - timer.remaining / timer.total : 1

  return (
    <div className={timer.done ? `${styles.bar} ${styles.done}` : styles.bar}>
      <div className={styles.progress} style={{ transform: `scaleX(${progress})` }} />

      <span className={styles.label}>{timer.done ? 'Отдых окончен' : 'Отдых'}</span>
      <span className={styles.time}>{formatTime(timer.remaining)}</span>

      {!timer.done && (
        <button className={styles.action} onClick={timer.running ? onPause : onResume}>
          {timer.running ? 'Пауза' : 'Дальше'}
        </button>
      )}

      <button className={styles.close} onClick={onDismiss} aria-label="Закрыть таймер">✕</button>
    </div>
  )
}
```

- [ ] **Step 3: Написать `src/components/RestTimer.module.css`**

```css
.bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 10;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 16px calc(14px + env(safe-area-inset-bottom));
  background: var(--surface-2);
  border-top: 1px solid var(--line);
  overflow: hidden;
}

.done { border-top-color: var(--accent); }

.progress {
  position: absolute;
  inset: 0;
  transform-origin: left;
  background: var(--accent);
  opacity: 0.14;
  transition: transform 1s linear;
}

.label, .time, .action, .close { position: relative; }

.label { font-size: 14px; color: var(--text-dim); }

.time {
  font-size: 26px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}

.action {
  min-height: 44px;
  margin-left: auto;
  padding: 0 16px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 10px;
}

.close {
  min-width: 44px;
  min-height: 44px;
  color: var(--text-dim);
  font-size: 18px;
}

.done .action { display: none; }
```

- [ ] **Step 4: Прогнать тесты и собрать**

Run: `npm test && npm run build`
Expected: тесты зелёные, сборка проходит.

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: панель таймера отдыха"
```

---

### Task 12: Экран тренировки

**Files:**
- Create: `src/hooks/useWorkout.js`
- Create: `src/components/AthleticBlock.jsx`, `src/components/AthleticBlock.module.css`
- Create: `src/components/WorkoutScreen.jsx`, `src/components/WorkoutScreen.module.css`
- Modify: `src/App.jsx` (ветка `workout`)

**Interfaces:**
- Consumes: всё из `lib/workout.js`, `lib/storage.js`, `lib/progression.js`; компоненты из задач 10 и 11
- Produces:
  - `useWorkout(): {state, actions}` где `actions` — обёртки над `lib/workout.js`, каждая пишет в `localStorage`
  - `<AthleticBlock items exercises done onToggle onOpen />`
  - `<WorkoutScreen day exercises ... />`

- [ ] **Step 1: Написать `src/hooks/useWorkout.js`**

Хук — только проводка: состояние держит `useState`, все переходы делает `lib/workout.js`, после каждого перехода состояние сохраняется.

```js
import { useCallback, useState } from 'react'
import { loadState, saveState } from '../lib/storage.js'
import * as workout from '../lib/workout.js'

export function useWorkout() {
  const [state, setState] = useState(loadState)

  const apply = useCallback((fn) => {
    setState((prev) => {
      const next = fn(prev)
      saveState(next)
      return next
    })
  }, [])

  return {
    state,
    startWorkout: useCallback((dayId) => {
      apply((s) => workout.startWorkout(s, dayId, new Date().toISOString()))
    }, [apply]),

    setWeight: useCallback((id, weight) => {
      apply((s) => workout.setWeight(s, id, weight))
    }, [apply]),

    closeSet: useCallback((id, index, sets, reps) => {
      apply((s) => workout.closeSet(s, id, index, sets, reps))
    }, [apply]),

    clearSet: useCallback((id, index, sets) => {
      apply((s) => workout.clearSet(s, id, index, sets))
    }, [apply]),

    toggleAthletic: useCallback((id) => {
      apply((s) => workout.toggleAthletic(s, id))
    }, [apply]),

    finishWorkout: useCallback(() => {
      apply((s) => workout.finishWorkout(s, new Date().toISOString().slice(0, 10)))
    }, [apply]),
  }
}
```

- [ ] **Step 2: Написать `src/components/AthleticBlock.jsx`**

```jsx
import ExerciseGif from './ExerciseGif.jsx'
import styles from './AthleticBlock.module.css'

export default function AthleticBlock({ items, exercises, done, onToggle, onOpen }) {
  return (
    <section className={styles.block}>
      <h2 className={styles.heading}>Атлетический финишер</h2>
      <p className={styles.note}>5–10 минут. Не в ущерб восстановлению.</p>

      <ul className={styles.list}>
        {items.map((item) => {
          const exercise = exercises[item.exercise]
          const isDone = Boolean(done?.[item.exercise])
          return (
            <li key={item.exercise} className={isDone ? `${styles.card} ${styles.done}` : styles.card}>
              <button className={styles.gifBtn} onClick={() => onOpen(item.exercise)} aria-label={`Техника: ${exercise.name}`}>
                <ExerciseGif id={item.exercise} name={exercise.name} />
              </button>

              <div className={styles.meta}>
                <span className={styles.name}>{exercise.name}</span>
                <span className={styles.format}>{item.format} · отдых {item.rest} с</span>
              </div>

              <button
                className={styles.check}
                onClick={() => onToggle(item.exercise)}
                aria-label={isDone ? 'Снять отметку' : 'Отметить выполненным'}
              >
                {isDone ? '✓' : ''}
              </button>
            </li>
          )
        })}
      </ul>
    </section>
  )
}
```

- [ ] **Step 3: Написать `src/components/AthleticBlock.module.css`**

```css
.block {
  margin-top: 28px;
  padding: 18px 16px;
  background: var(--surface-2);
  border-radius: var(--radius);
}

.heading {
  margin: 0;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent);
}

.note {
  margin: 6px 0 16px;
  font-size: 13px;
  color: var(--text-dim);
}

.list { list-style: none; margin: 0; padding: 0; display: grid; gap: 10px; }

.card {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 10px;
}

.done { border-color: var(--accent); }

.gifBtn { padding: 0; flex: none; }

.meta { display: grid; gap: 2px; min-width: 0; }

.name { font-size: 16px; font-weight: 600; }

.format { font-size: 13px; color: var(--text-dim); }

.check {
  width: var(--tap);
  height: var(--tap);
  margin-left: auto;
  flex: none;
  display: grid;
  place-items: center;
  font-size: 22px;
  color: var(--accent-ink);
  background: var(--surface-2);
  border: 2px solid var(--line);
  border-radius: 50%;
}

.done .check { background: var(--accent); border-color: var(--accent); }
```

- [ ] **Step 4: Написать `src/components/WorkoutScreen.jsx`**

```jsx
import ExerciseRow from './ExerciseRow.jsx'
import AthleticBlock from './AthleticBlock.jsx'
import RestTimer from './RestTimer.jsx'
import { suggestWeight } from '../lib/progression.js'
import { useRestTimer } from '../hooks/useRestTimer.js'
import styles from './WorkoutScreen.module.css'

export default function WorkoutScreen({
  day, exercises, state, onStart, onWeight, onCloseSet, onClearSet, onToggleAthletic, onFinish, onOpen, onBack,
}) {
  const rest = useRestTimer()
  const current = state.current?.dayId === day.id ? state.current : null

  const suggestions = day.blocks
    .map((block) => {
      const weight = suggestWeight(
        state.lastSession[block.exercise],
        block,
        exercises[block.exercise].weightStep,
        current?.weights?.[block.exercise] ?? null,
      )
      return weight == null ? null : { block, weight }
    })
    .filter(Boolean)

  function handleClose(exerciseId, index, sets, reps, restSeconds) {
    onCloseSet(exerciseId, index, sets, reps)
    rest.startRest(restSeconds)
  }

  return (
    <div className={styles.screen}>
      <header className={styles.header}>
        <button className={styles.back} onClick={onBack}>← Дни</button>
        <h1 className={styles.title}>{day.weekday}</h1>
        <p className={styles.accent}>{day.accent}</p>
      </header>

      {!current && (
        <button className={styles.start} onClick={() => onStart(day.id)}>
          Начать тренировку
        </button>
      )}

      {current && (
        <>
          <div className={styles.rows}>
            {day.blocks.map((block) => (
              <ExerciseRow
                key={block.exercise}
                block={block}
                exercise={exercises[block.exercise]}
                exerciseId={block.exercise}
                reps={current.reps[block.exercise]}
                weight={current.weights[block.exercise] ?? null}
                lastSession={state.lastSession[block.exercise]}
                onWeight={(w) => onWeight(block.exercise, w)}
                onClose={(i, r) => handleClose(block.exercise, i, block.sets, r, block.rest)}
                onEdit={(i, r) => onCloseSet(block.exercise, i, block.sets, r)}
                onClear={(i) => onClearSet(block.exercise, i, block.sets)}
                onOpen={() => onOpen(block.exercise)}
              />
            ))}
          </div>

          <AthleticBlock
            items={day.athletic}
            exercises={exercises}
            done={current.athletic}
            onToggle={onToggleAthletic}
            onOpen={onOpen}
          />

          <button className={styles.finish} onClick={onFinish}>Завершить тренировку</button>
        </>
      )}

      {/* Спека, раздел 7: плашка итогов живёт внизу экрана */}
      {suggestions.length > 0 && (
        <section className={styles.suggestions}>
          <h2 className={styles.suggestHeading}>Пора добавить вес</h2>
          {suggestions.map(({ block, weight }) => (
            <p key={block.exercise} className={styles.suggestion}>
              <strong>{exercises[block.exercise].name}</strong> — все подходы на {block.reps[1]}.
              В следующий раз {weight} кг.
            </p>
          ))}
        </section>
      )}

      <RestTimer
        timer={rest.timer}
        onPause={rest.pauseRest}
        onResume={rest.resumeRest}
        onDismiss={rest.dismissRest}
      />
    </div>
  )
}
```

- [ ] **Step 5: Написать `src/components/WorkoutScreen.module.css`**

Нижний отступ рассчитан так, чтобы панель таймера не перекрывала кнопку «Завершить».

```css
.screen {
  max-width: 560px;
  margin: 0 auto;
  padding: 16px 16px calc(140px + env(safe-area-inset-bottom));
}

.header { margin-bottom: 20px; }

.back {
  min-height: var(--tap);
  padding: 0 4px;
  color: var(--text-dim);
  font-size: 16px;
}

.title { margin: 8px 0 2px; font-size: 26px; }

.accent { margin: 0; color: var(--accent); font-size: 15px; }

.suggestions {
  margin-top: 24px;
  padding: 14px 16px;
  background: var(--surface);
  border-left: 3px solid var(--accent);
  border-radius: 0 var(--radius) var(--radius) 0;
}

.suggestHeading {
  margin: 0 0 8px;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent);
}

.suggestion { margin: 0 0 6px; font-size: 15px; line-height: 1.45; }

.suggestion:last-child { margin-bottom: 0; }

.start, .finish {
  width: 100%;
  min-height: 60px;
  font-size: 17px;
  font-weight: 600;
  border-radius: var(--radius);
}

.start { color: var(--accent-ink); background: var(--accent); }

.finish {
  margin-top: 24px;
  color: var(--text);
  background: var(--surface);
  border: 1px solid var(--line);
}

.rows { display: grid; gap: 14px; }
```

- [ ] **Step 6: Подключить экран в `src/App.jsx`**

Полная новая версия файла:

```jsx
import { useEffect, useState } from 'react'
import { parseHash, subscribeRoute, navigate } from './lib/router.js'
import { todayDayId, findDay } from './lib/program.js'
import { useWorkout } from './hooks/useWorkout.js'
import program from './data/program.json'
import exercises from './data/exercises.json'
import DayList from './components/DayList.jsx'
import WorkoutScreen from './components/WorkoutScreen.jsx'
import ExerciseDetail from './components/ExerciseDetail.jsx'

export default function App() {
  const [route, setRoute] = useState(() => parseHash(window.location.hash))
  const w = useWorkout()

  useEffect(() => subscribeRoute(setRoute), [])

  if (route.screen === 'workout') {
    const day = findDay(program, route.dayId)
    if (!day) {
      navigate({ screen: 'days' })
      return null
    }
    return (
      <WorkoutScreen
        day={day}
        exercises={exercises}
        state={w.state}
        onStart={w.startWorkout}
        onWeight={w.setWeight}
        onCloseSet={w.closeSet}
        onClearSet={w.clearSet}
        onToggleAthletic={w.toggleAthletic}
        onFinish={() => { w.finishWorkout(); navigate({ screen: 'days' }) }}
        onOpen={(exerciseId) => navigate({ screen: 'exercise', exerciseId })}
        onBack={() => navigate({ screen: 'days' })}
      />
    )
  }

  if (route.screen === 'exercise') {
    const exercise = exercises[route.exerciseId]
    if (!exercise) {
      navigate({ screen: 'days' })
      return null
    }
    return (
      <ExerciseDetail
        exercise={exercise}
        exerciseId={route.exerciseId}
        onBack={() => window.history.back()}
      />
    )
  }

  return (
    <DayList
      program={program}
      todayId={todayDayId(new Date())}
      onPick={(dayId) => navigate({ screen: 'workout', dayId })}
    />
  )
}
```

- [ ] **Step 7: Проверить сквозной сценарий в браузере**

Run: `npm run dev`

Пройти руками и убедиться в каждом пункте:
1. Открыть день → «Начать тренировку» → появляется список из шести упражнений.
2. Вписать вес 60 в жим лёжа.
3. Тапнуть первый кружок → он заливается зелёным с числом 8, снизу выезжает таймер на 3:00 и идёт вниз.
4. Тапнуть по залитому кружку → появляется степпер, «−» уменьшает число до 7, «Сбросить» возвращает кружок в пустое состояние.
5. Закрыть все три подхода на 8, отметить финишер, нажать «Завершить тренировку» → возврат к списку дней.
6. Снова открыть тот же день → сверху плашка «Пора добавить вес: Жим штанги лёжа — все подходы на 8. В следующий раз 62.5 кг».
7. Перезагрузить страницу посреди тренировки → отметки на месте.

- [ ] **Step 8: Прогнать тесты, собрать, закоммитить**

```bash
npm test && npm run build
git add -A
git commit -m "feat: экран тренировки с подходами, финишером и прогрессией"
```

---

### Task 13: Офлайн и установка на главный экран

**Files:**
- Modify: `vite.config.js`
- Create: `public/icons/icon.svg`, `public/icons/icon-192.png`, `public/icons/icon-512.png`

**Interfaces:**
- Consumes: собранный `dist/`
- Produces: работающий service worker, манифест, установка иконкой

- [ ] **Step 1: Нарисовать `public/icons/icon.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="112" fill="#0f1115"/>
  <rect x="96" y="232" width="320" height="48" rx="24" fill="#c6f24e"/>
  <rect x="120" y="184" width="64" height="144" rx="28" fill="#c6f24e"/>
  <rect x="328" y="184" width="64" height="144" rx="28" fill="#c6f24e"/>
  <rect x="72" y="216" width="40" height="80" rx="18" fill="#c6f24e"/>
  <rect x="400" y="216" width="40" height="80" rx="18" fill="#c6f24e"/>
</svg>
```

- [ ] **Step 2: Сконвертировать в PNG**

`qlmanage` и `sips` — встроенные утилиты macOS, ставить ничего не нужно.

```bash
cd /var/www/arvix/public/icons
qlmanage -t -s 512 -o . icon.svg
mv icon.svg.png icon-512.png
cp icon-512.png icon-192.png
sips -z 192 192 icon-192.png
```

Проверить: `sips -g pixelWidth -g pixelHeight icon-512.png icon-192.png`
Expected: 512×512 и 192×192.

Если `qlmanage` не отработал — иконка чисто косметическая, подойдёт любой PNG нужного размера. Это не повод останавливать задачу.

- [ ] **Step 3: Подключить `vite-plugin-pwa` в `vite.config.js`**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['gifs/**/*.gif', 'icons/*.png'],
      manifest: {
        name: 'Arvix — тренажёрный помощник',
        short_name: 'Arvix',
        description: 'Техника, подходы и прогрессия под рукой в зале',
        lang: 'ru',
        start_url: '/',
        scope: '/',
        display: 'standalone',
        orientation: 'portrait',
        background_color: '#0f1115',
        theme_color: '#0f1115',
        icons: [
          { src: 'icons/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: 'icons/icon-512.png', sizes: '512x512', type: 'image/png' },
          { src: 'icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
        ],
      },
      workbox: {
        // Гифки тяжелее стандартного лимита в 2 МБ — поднимаем, иначе они
        // не попадут в предварительный кеш и в зале без сети не откроются.
        globPatterns: ['**/*.{js,css,html,json,png,svg,gif}'],
        maximumFileSizeToCacheInBytes: 8 * 1024 * 1024,
      },
    }),
  ],
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

- [ ] **Step 4: Собрать и проверить, что service worker появился**

```bash
npm run build
ls dist/sw.js dist/manifest.webmanifest
```

Expected: оба файла существуют.

- [ ] **Step 5: Проверить, что данные и иконки попали в предкеш**

```bash
grep -c 'icon-512' dist/sw.js
```

Expected: не ноль. Список предкеша лежит внутри `dist/sw.js`; если гифок ещё нет в `public/gifs/`, их там не будет — это нормально, они добавятся при следующей сборке.

- [ ] **Step 6: Проверить офлайн вручную**

```bash
npm run preview
```

Открыть адрес из вывода, дать странице загрузиться, затем в DevTools → Network включить Offline и перезагрузить.
Expected: приложение открывается, список дней на месте, начатая тренировка сохранена.

- [ ] **Step 7: Коммит**

```bash
git add -A
git commit -m "feat: офлайн-режим и установка на главный экран"
```

---

### Task 14: Гифки и деплой

**Files:**
- Create: `public/gifs/*.gif` (22 файла)
- Create: `README.md`

**Interfaces:**
- Consumes: список из 22 идентификаторов (задача 7)
- Produces: задеплоенный сайт

⚠️ **Деплой требует аккаунта пользователя.** Шаги 4–6 выполняются только после явного согласия — это публикация в интернет, а не локальное действие.

- [ ] **Step 1: Собрать гифки**

Положить файлы в `public/gifs/` с именами точно по идентификаторам из таблицы задачи 7: `bench-press.gif`, `db-row.gif`, `pull-up.gif`, `incline-db-press.gif`, `bulgarian-split-squat.gif`, `leg-curl.gif`, `back-squat.gif`, `db-rdl.gif`, `leg-press.gif`, `db-bench-press.gif`, `lateral-raise.gif`, `seated-db-press.gif`, `seated-row.gif`, `db-curl.gif`, `triceps-pushdown.gif`, `bosu-balance.gif`, `band-rotation.gif`, `jump-rope.gif`, `agility-ladder.gif`, `medball-slam.gif`, `kettlebell-swing.gif`, `squat-jump.gif`.

Недостающие файлы приложение переживает — показывает плашку с буквой. Можно деплоить с частичным набором и дополнять по одной.

- [ ] **Step 2: Проверить соответствие имён**

```bash
cd /var/www/arvix
node -e "
const ex = require('./src/data/exercises.json');
const fs = require('fs');
const have = new Set(fs.readdirSync('public/gifs').filter(f => f.endsWith('.gif')).map(f => f.replace('.gif','')));
const missing = Object.keys(ex).filter(id => !have.has(id));
const extra = [...have].filter(id => !ex[id]);
console.log('нет гифки:', missing.join(', ') || '—');
console.log('лишние файлы:', extra.join(', ') || '—');
"
```

Expected: «лишние файлы: —». Список в «нет гифки» допустим — это то, что осталось найти.

- [ ] **Step 3: Написать `README.md`**

```markdown
# Arvix

Личный офлайн-помощник для зала: техника упражнения, норма подходов, отметки и двойная прогрессия.

## Команды

    npm install     установка
    npm run dev     локальная разработка
    npm test        тесты
    npm run build   сборка в dist/
    npm run preview просмотр собранного

## Как менять программу

`src/data/program.json` — что и сколько делать. Дни, упражнения, подходы, диапазон повторений, RIR, отдых, темп.

`src/data/exercises.json` — как делать. Техника, частые ошибки, целевые мышцы, шаг прибавки веса.

Новое упражнение: запись в `exercises.json` + файл `public/gifs/<id>.gif` + строка в `program.json`. Кода не касается.

Тест `src/data/data.test.js` следит, чтобы программа и библиотека не разъезжались.

## Прогресс

Хранится в `localStorage` браузера, ключ `arvix.v1`. По каждому упражнению — только последняя сессия: этого хватает для двойной прогрессии.
```

- [ ] **Step 4: Спросить пользователя перед деплоем**

Показать, что сборка готова, и спросить, куда деплоить и от какого аккаунта. Без ответа дальше не идти.

- [ ] **Step 5: Задеплоить на Vercel**

```bash
cd /var/www/arvix
npx vercel --prod
```

Vercel определит Vite автоматически: команда сборки `npm run build`, каталог `dist`. Потребуется вход в аккаунт.

Если выбран GitHub Pages вместо Vercel — в `vite.config.js` добавить `base: '/arvix/'`, иначе ассеты не найдутся на подпути.

- [ ] **Step 6: Проверить на телефоне**

Открыть выданный адрес, добавить на главный экран, включить авиарежим, запустить с иконки.
Expected: приложение открывается без сети, гифки на месте, тренировка стартует.

- [ ] **Step 7: Коммит**

```bash
git add -A
git commit -m "feat: гифки упражнений и README"
```

---

## Self-Review

**1. Покрытие спеки**

| Требование спеки | Задача |
|---|---|
| Три дня с силовым блоком и финишером (§3) | 7, 12 |
| Карточка техники: анимация, шаги, ошибки, мышцы (§3, §7) | 9 |
| Отметка подходов, поле веса, «в прошлый раз» (§3, §7) | 10, 12 |
| Таймер отдыха с автозапуском и вибрацией (§3, §7) | 11, 12 |
| Расчёт двойной прогрессии и плашка (§3, §7, §8) | 3, 12 |
| Установка иконкой, работа офлайн (§3, §9) | 13 |
| Два JSON вместо одного (§4, §5.1, §5.2) | 7 |
| Хеш-роутинг ради кнопки «назад» (§4) | 5 |
| Схема `localStorage`, только последняя сессия (§5.3) | 2, 6 |
| Битый JSON не роняет приложение (§5.3) | 2 |
| Конкретная программа из §6 | 7 |
| Заглушка вместо отсутствующей гифки (§9) | 9 |
| Тесты `progression`, `storage`, `workout`, таймер (§10) | 3, 2, 6, 4 |

Пробелов не осталось. Пункты «не входит в v1» (§3) задач не имеют — так и задумано.

**2. Заглушки**

Проверено: «TBD», «реализовать позже», «аналогично задаче N» и шагов без кода в плане нет. Единственное место, где план задаёт содержание образцом, а не дословно, — прозаические поля 22 упражнений в задаче 7. Там структурные поля выписаны таблицей полностью, дан полный эталон формата на двух записях, а качество текстов проверяется тестом с числовыми порогами (шагов ≥ 3, ошибок ≥ 2, длина шага > 15 символов). Это осознанный размен: выписывать 22 набора техники дословно означало бы утроить объём плана без выигрыша в определённости.

**3. Согласованность типов**

Проверено сквозняком по задачам:
- `suggestWeight(lastSession, block, weightStep, currentWeight)` — сигнатура из задачи 3 совпадает с вызовом в `WorkoutScreen` (задача 12).
- `closeSet(state, exerciseId, setIndex, sets, repsValue)` — порядок аргументов в `lib/workout.js` (задача 6), в `useWorkout` (задача 12) и в колбэках `ExerciseRow` (задача 10) одинаковый.
- `formatTime` импортируется из `lib/timer.js` и в `RestTimer`, и в `ExerciseRow` — имя одно.
- `<ExerciseGif id name size />` — проп `size` со значениями `'row' | 'detail'` используется одинаково в задачах 9, 10, 12.
- `current.athletic` заводится в `startWorkout` (задача 6), читается в `AthleticBlock` (задача 12) — поле одно и то же.
- `block.reps` везде массив `[min, max]`, строкового варианта нигде нет.
- `clearSet(state, exerciseId, setIndex, sets)` из задачи 6 доходит до UI: `useWorkout.clearSet` → `WorkoutScreen.onClearSet` → `ExerciseRow.onClear` → кнопка «Сбросить» в `SetTracker`. Мёртвых экспортов в `lib/` не осталось.
- Плашка прогрессии рендерится последней в `WorkoutScreen`, как требует спека §7 («внизу экрана»).
