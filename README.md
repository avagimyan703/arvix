# Arvix

Личный офлайн-помощник для зала: техника упражнения, норма подходов, отметки и двойная прогрессия. Один пользователь, без сервера и аккаунтов.

## Команды

```
npm install      установка
npm run dev      локальная разработка
npm test         тесты
npm run build    сборка в dist/
npm run preview  просмотр собранного
```

## Как менять программу

`src/data/program.json` — что и сколько делать. Дни, упражнения, подходы, диапазон повторений, RIR, отдых, темп.

`src/data/exercises.json` — как делать. Техника, частые ошибки, целевые мышцы, шаг прибавки веса.

Новое упражнение: запись в `exercises.json` + файл `public/gifs/<id>.gif` + строка в `program.json`. Кода это не касается.

Тест `src/data/data.test.js` следит, чтобы программа и библиотека не разъезжались в обе стороны: упражнение из программы обязано быть в библиотеке, а неиспользуемых записей в библиотеке быть не должно.

## Каталог рилсов

`src/data/reels.json` — библиотека разборов: категории по группам мышц и сами рилсы, ключом служит короткий код Инстаграма из ссылки.

```json
{
  "categories": [{ "id": "back", "name": "Спина" }],
  "reels": {
    "DbTgI_yCUor": {
      "category": "back",
      "note": "Одна и та же тяга, разное включение спины",
      "author": "demicstory",
      "url": "https://www.instagram.com/reel/DbTgI_yCUor/"
    }
  }
}
```

Каталог доступен с главного экрана: `#/reels` — категории со счётчиками (пустые приглушены и не нажимаются), `#/reels/<категория>` — список разборов, плеер раскрывается по тапу. Раскрыт всегда только один: десяток iframe'ов Инстаграма на экране съел бы всю память телефона.

Упражнение ссылается на рилс ключом: `"video": "DbTgI_yCUor"` в `exercises.json`. Один рилс так можно повесить на несколько упражнений, не копируя его. Тест данных проверяет, что ключ существует в библиотеке, категория известна, а сам ключ совпадает с кодом в ссылке.

Встраивается через iframe на официальный `/<reel|p>/<id>/embed`, а не через `embed.js`: скрипт грузился бы в наше окно и тащил трекинг внутрь приложения. Обвязка Инстаграма скрыта с двух сторон — шапка сдвигом вверх на 54px, нижняя панель за счёт height больше контейнера.

**Офлайн.** Плеер без сети не работает — это осознанный размен на решение убрать гифки. `useOnline` проверяет связь заранее, потому что cross-origin iframe о своей неудаче не сообщает; вместо пустого прямоугольника показывается текст. Разбор техники, ошибки и целевые мышцы остаются в кеше и читаются без сети.

## Гифки (удалены)

Раньше в `public/gifs/` лежали 21 анимация из wger и free-exercise-db. Удалены — вместо них рилсы. Если понадобится вернуть, коммиты `0ce610d`, `033a55e` и `feb2cdd` содержат и файлы, и скрипты сборки.

Прежний раздел про источники оставлен ниже как справка, но к текущему состоянию не относится.

## Прежние источники гифок

Кладутся в `public/gifs/` с именем, равным идентификатору упражнения: `back-squat.gif`, `bench-press.gif` и так далее. Полный список идентификаторов — ключи в `exercises.json`.

Отсутствующий файл приложение переживает: показывает плашку с первой буквой названия. Можно деплоить с неполным набором и дополнять по одной.

Набор собран из двух источников, оба со внятной лицензией.

**11 плавных из wger** — нарезаны из видео [wger](https://wger.de) (автор Goulart, CC BY-SA 4.0) скриптом `scripts/wger-gifs.sh`: `bench-press`, `db-bench-press`, `incline-db-press`, `pull-up`, `leg-curl`, `leg-press`, `lateral-raise`, `seated-db-press`, `seated-row`, `db-curl`, `triceps-pushdown`.

**1 плавная с Викисклада** — `back-squat`, из [Wikimedia Commons](https://commons.wikimedia.org) (автор FitnessScape, CC BY 3.0). Список доступных видео там смотреть скриптом `scripts/list-commons-videos.js`.

Все двенадцать плавных — по 36 кадров, 12 fps, 3 секунды. Обе лицензии требуют атрибуции: она стоит в подвале главного экрана, убирать её нельзя.

**9 двухкадровых** — склеены из пар «начальная позиция / конечная» базы [free-exercise-db](https://github.com/yuhonas/free-exercise-db) скриптом `scripts/build-gifs.sh`.

На Викискладе в той же серии есть ещё «Deadlift» и «Bent-over row», но они сознательно не взяты: там классическая тяга с пола и тяга штанги в наклоне с неопертым корпусом, а в программе стоят румынская тяга с гантелями и тяга гантели в упоре. Показывать не то упражнение, зато плавно — хуже, чем нужное, зато моргающее.

Пять из двухкадровых приблизительные — движение то же, но снаряд или вариант другой: `bulgarian-split-squat`, `bosu-balance`, `band-rotation`, `medball-slam`, `kettlebell-swing`. Координационной лестницы нет ни в одном источнике. Эти шесть — первые кандидаты на свою съёмку.

Общий вес гифок около 11 МБ, и все они уходят в офлайн-кеш при первом заходе.

Проверить, каких гифок не хватает:

```bash
node -e "
const ex = require('./src/data/exercises.json');
const fs = require('fs');
const have = new Set(fs.readdirSync('public/gifs').filter(f => f.endsWith('.gif')).map(f => f.replace('.gif','')));
console.log('нет гифки:', Object.keys(ex).filter(id => !have.has(id)).join(', ') || '—');
console.log('лишние файлы:', [...have].filter(id => !ex[id]).join(', ') || '—');
"
```

## Видео

У упражнения может быть необязательное поле `video`:

```json
"video": {
  "url": "https://www.instagram.com/reel/…/",
  "note": "Одна и та же тяга, разное включение спины",
  "author": "demicstory"
}
```

Тогда на экране техники **вместо гифки** играет встроенный плеер. Гифка не исчезает: без сети плеер отрисовался бы пустым, поэтому офлайн на его месте снова она. Так что видео вытесняет гифку только когда его реально можно показать.

Встраивается через iframe на официальный `/<reel|p>/<id>/embed`, а не через `embed.js`: скрипт грузился бы в наше окно и тащил трекинг внутрь приложения. Обвязка Инстаграма скрыта с двух сторон — шапка сдвигом вверх на 54px, нижняя панель за счёт height больше контейнера. Автор при этом указан строкой под названием упражнения.

### Куда какое видео вешать

Присылая видео, достаточно назвать группу мышц. Соответствие групп упражнениям:

| Группа | Упражнения |
|---|---|
| Грудь | `bench-press`, `db-bench-press`, `incline-db-press` |
| Спина | `pull-up`, `db-row`, `seated-row` |
| Плечи | `seated-db-press`, `lateral-raise` |
| Бицепс | `db-curl` |
| Трицепс | `triceps-pushdown` |
| Ноги (квадрицепс) | `back-squat`, `leg-press`, `bulgarian-split-squat` |
| Ноги (задняя цепь) | `db-rdl`, `leg-curl` |
| Атлетический блок | `jump-rope`, `agility-ladder`, `medball-slam`, `kettlebell-swing`, `squat-jump`, `bosu-balance`, `band-rotation` |

Группы вроде «трицепс» однозначны, «спина» и «ноги» — нет. Если видео про конкретное движение, лучше назвать упражнение; если это общий разбор группы, его можно повесить на все её упражнения.

## Прогресс

Хранится в `localStorage`, ключ `arvix.v1`. По каждому упражнению — только последняя сессия: этого хватает для двойной прогрессии, а данные остаются крошечными.

Правило прогрессии (`src/lib/progression.js`): подсказка «пора добавить вес» появляется, когда во всех плановых подходах достигнута верхняя граница диапазона и вес сегодня не ниже прошлого. Ничего не меняется автоматически — поле веса остаётся за тобой.

## Иконки

PNG в `public/icons/` собраны из SVG в `icons-src/`. Пересобрать после правки исходников:

```bash
cd public/icons
qlmanage -t -s 512 -o . ../../icons-src/icon.svg && mv icon.svg.png icon-512.png
cp icon-512.png icon-192.png && sips -z 192 192 icon-192.png
qlmanage -t -s 512 -o . ../../icons-src/icon-maskable.svg && mv icon-maskable.svg.png icon-512-maskable.png
```

## Документы

- Спека: `docs/superpowers/specs/2026-07-29-arvix-design.md`
- План реализации: `docs/superpowers/plans/2026-07-29-arvix-v1.md`
